# Runtime dependencies for the workshop.
#
# These are the exact versions the workshop was recorded and verified with
# (see docs/01-getting-started.md for install instructions).  Everything else
# a scene needs arrives transitively through manim.
#
# For byte-for-byte reproduction of the author's machine — including notebook
# tooling and test utilities — use requirements-lock.txt instead.
manim==0.21.0
numpy==2.5.3
opencv-python==5.0.0.93
